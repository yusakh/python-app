apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-issuer
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pe-backstage-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: pe-backstage-ca
  secretName: pe-backstage-ca-secret
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
    group: cert-manager.io
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: pe-backstage-ca-issuer
spec:
  ca:
    secretName: pe-backstage-ca-secret
